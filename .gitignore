# Environment variables (keep local, never commit)
.env

# Autorestic configuration (may contain sensitive paths)
autorestic_conf.yaml

# Docker volumes and runtime data
docker-compose.override.yml

# OS files
.DS_Store
Thumbs.db
